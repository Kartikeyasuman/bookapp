{
  "buildCommand": "cd client && npm install && npm start",
  "outputDirectory": "client/build",
  "framework": "react",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {"source": "/(.*)", "destination": "/"}
  ]
}
